edition = "2024";

package proto;

message SidToIdMap {
	message Pair {
		bytes short_id = 1;
		string id = 2;
	}

	message Bucket {
		repeated Pair pairs = 1;
	}

	// i know a map type exists but doing it manually for fun
	repeated Bucket buckets = 1;
}
